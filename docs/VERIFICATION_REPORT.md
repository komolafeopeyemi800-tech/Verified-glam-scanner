# Verification report (automated)

Generated by agent run of FCM + E2E plan. Re-run: `.\tools\run-full-verification.ps1`

## Backend E2E (10 scan features + guide)

| Feature | Result | Notes |
|---------|--------|-------|
| FACE_BEAUTY_ANALYSIS | PASS | analyze-scan v=2 |
| COLOR_ANALYSIS | PASS | |
| BEAUTY_TIPS | PASS | |
| GLOW_UP_GUIDE | PASS | |
| CELEBRITY_LOOKALIKE | PASS | |
| FACIAL_SYMMETRY | PASS | |
| BEAUTY_SCORE_SHOWDOWN | PASS | |
| FACIAL_RESEMBLANCE | PASS | |
| FACE_READING | PASS | |
| GOLDEN_RATIO | PASS | |
| guide-recommendations | PASS | 5 tips |

Command: `.\tools\e2e-test-supabase.ps1`  
Fixture: `tools/fixtures/test-face.jpg` (see `tools/fixtures/README.md`)

## FCM push backend

| Check | Result |
|-------|--------|
| send-challenge-push deployed | FAIL (404) |
| dispatch-challenge-notifications deployed | FAIL (404) |
| FCM_SERVICE_ACCOUNT_JSON secret | Not set via CLI (needs token in `.env`) |

Fix:

1. Uncomment `SUPABASE_ACCESS_TOKEN=sbp_...` in `.env` (real token, not placeholder).
2. `.\scripts\setup-fcm-push.ps1`
3. `.\scripts\apply-migration-007.ps1`
4. Dashboard: cron on `dispatch-challenge-notifications` every 10 min.
5. `.\tools\test-challenge-push-dispatch.ps1` → expect HTTP 200.

Or set `FCM_SERVICE_ACCOUNT_JSON` manually in Dashboard and run `.\scripts\deploy-challenge-push-functions.ps1` only.

## Production build

| Check | Result |
|-------|--------|
| Release APK with production dart-defines | PASS |
| Output | `build/app/outputs/flutter-apk/app-release.apk` (~97 MB) |

Uses `.\scripts\build-release-apk.ps1` with `--no-tree-shake-icons` (Windows Flutter const_finder workaround). Fixed `vg_copy.dart` missing `vgAppName` import for release compile.

## Phone checklist (manual)

See [FCM_PUSH_SETUP.md](FCM_PUSH_SETUP.md) device test section and [SUPABASE_SETUP.md](SUPABASE_SETUP.md) phone matrix.
